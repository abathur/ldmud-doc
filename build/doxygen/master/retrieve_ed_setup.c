public class master {
   /**
    * Should return an integer that gives the ed setup flags for the user that is denoted by command_giver.
    * 
    * @synopsis{
    * int retrieve_ed_setup(object command_giver)
    * }
    * 
    * @see master::save_ed_setup()
   **/
   int retrieve_ed_setup(object command_giver);
}
