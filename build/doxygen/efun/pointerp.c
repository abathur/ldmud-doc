public class efun {
   /**
    * Return 1 if arg is a pointer, i.e. an array.
    * 
    * @synopsis{
    * int pointerp(mixed arg)
    * }
    * 
    * @see efun::clonep(), efun::closurep(), efun::floatp(), efun::mappingp(), efun::objectp(), efun::intp(), efun::referencep(), efun::stringp(), efun::symbolp(), efun::structp()
   **/
   int pointerp(mixed arg);
}
