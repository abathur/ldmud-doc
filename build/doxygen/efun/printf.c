public class efun {
   /**
    * A cross between efun::sprintf() and efun::write(). Returns void and prints the result string to the user.
    * 
    * @synopsis{
    * void printf(string format, ...)
    * }
    * 
    * @see efun::sprintf(), efun::write(), efun::terminal_colour()
   **/
   void printf(string format, ...);
}
