public class efun {
   /**
    * Return 1 if <arg> is a string.
    * 
    * @synopsis{
    * int stringp(mixed arg)
    * }
    * 
    * @see efun::clonep(), efun::closurep(), efun::floatp(), efun::mappingp(), efun::objectp(), efun::intp(), efun::referencep(), efun::pointerp(), efun::symbolp(), efun::structp()
   **/
   int stringp(mixed arg);
}
