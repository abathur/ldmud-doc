public class efun {
   /**
    * Make a directory named path. Return 1 for success and 0 for failure.
    * 
    * @synopsis{
    * int mkdir(string path)
    * }
    * 
    * @see efun::rmdir(), efun::rm(), efun::get_dir(), efun::file_size()
   **/
   int mkdir(string path);
}
