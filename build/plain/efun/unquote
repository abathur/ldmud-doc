SYNOPSIS
========
   mixed  *        unquote(quoted_array)
   string|  symbol unquote(symbol      )

DESCRIPTION
===========
   Removes a quote from quoted arrays and symbols. When the last quote from a 
   symbol is removed, the result is a string.

   USAGE
   =====
         unquote('foo)         // returns "foo"
         unquote( '({1,2,3}) ) // returns ({1,2,3})

HISTORY
=======
   - introduced (3.2.9)

SEE ALSO
========
   quote(E), symbolp(E)
