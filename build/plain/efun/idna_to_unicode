SYNOPSIS (OPTIONAL)
===================
   #include <idn.h>
   string idna_to_unicode(string name)

DESCRIPTION
===========
   Convert string name(E) from idna representation (8z punycode) to UTF-8.

   If an error occurs, an exception is thrown.

   The efun is available only if the system supports libidn - in that case 
   __IDNA__ is defined.

HISTORY
=======
   - introduced (3.3.713)

SEE ALSO
========
   convert_charset(E), idna_to_ascii(E), idna_stringprep(E)
