SYNOPSIS (DEPRECATED)
=====================
   int prevent_insert()

DESCRIPTION
===========
   In compat mode, this function is used by transfer(E).

   Define this function in objects that are neither living nor rooms if you 
   want to prevent to put something into current object.

   Return true if ok, otherwise 0. That means that default is that it is not 
   possible to put something into an object.

HISTORY
=======
   - deprecated (3.3) -- because transfer(E) has been deprecated.

SEE ALSO
========
   transfer(E), can_put_and_get(A)
