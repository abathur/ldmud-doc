SYNOPSIS
========
   int id(string str)

DESCRIPTION
===========
   Let the object identify itself. If str matches an id of the current object 
   then return a non-zero value.

   This lfun is applied for present()(E).

   Todo: this is now a false document, right?

   USAGE
   =====
      Return 1 if the object identifies to "sword" or "sword of fire"; 0 
      otherwise:

         int id(string str) {
           return "sword" == str || "sword of fire" == str;
         }

SEE ALSO
========
   present()(E)
