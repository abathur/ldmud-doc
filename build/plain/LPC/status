TYPE
====
   status

DESCRIPTION
===========
   Obsolete:  
     status was planned to be an optimized boolean format, but this was 
     never actually implemented. status does work; however, since it is only
     an alias for type 'int', just use int.

SEE ALSO
========
   int(LPC), types(LPC)
