.. command:: malloc

  This command is hardcoded into the driver's input parser.
  It shows the statistics of the memory management module.

  :history 3.2.7 changed: 'status malloc' now has the same effect

  .. seealso:: :driver:`status`, :concept:`memory`, :efun:`debug_info`
