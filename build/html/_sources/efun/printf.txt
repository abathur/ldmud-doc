.. efun:: void printf(string format, ...)

  A cross between sprintf() and write(). Returns void and prints
  the result string to the user.

  .. seealso:: :efun:`sprintf`, :efun:`write`, :efun:`terminal_colour`
