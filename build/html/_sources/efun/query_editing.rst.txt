.. efun:: mixed query_editing(object ob)

  Returns 1 if the :arg:`ob` is interactive and currently editing with :efun:`ed`. If :efun:`ed` was called with a function name as second argument, the object where :efun:`ed` was called is returned, else 0.

  .. seealso:: :efun:`ed`
